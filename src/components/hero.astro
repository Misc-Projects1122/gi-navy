---

---

<div class="md:relative border-y-4 border-primary">
  <div class="carousel_container">
    <div class="flex flex-nowrap overflow-hidden">
      <div class="carousel_item">
        <img src="/hero/heroBanner1.png" class="h-full w-full" />
      </div>
      <div class="carousel_item">
        <img src="/hero/heroBanner2.png" class="h-full w-full" />
      </div>
      <div class="carousel_item">
        <img src="/hero/heroBanner3.png" class="h-full w-full" />
      </div>
      <div class="carousel_item">
        <img src="/hero/heroBanner4.png" class="h-full w-full" />
      </div>
    </div>
  </div>
  <div class="md:absolute inset-0 bg-black opacity-60"></div>
  <div class="md:absolute inset-0 flex items-center justify-start md:px-16">
    <div
      class="text-white text-center md:text-left py-4 md:py-0 px-7 md:px-0 bg-dark md:bg-transparent max-w-2xl">
      <h1
        class="text-md font-bold md:text-5xl lg:text-6xl leading-tight font-barlow">
        Pioneering Engineering, Delivering Excellence
      </h1>
      <p class="mt-4 text-base md:text-lg lg:text-xl leading-relaxed">
        Mission-driven, customer-centric, multi-domain maritime solutions using
        cutting-edge technologies to envision and safeguard the fleet of the
        future.
      </p>
      <div class="mt-8 space-x-4">
        <a
          class="text-white border-2 border-white px-3 md:px-6 py-2 md:py-3 text-md md:text-lg inline-block">
          Learn More
        </a>
        <a
          href="#"
          class="bg-blue-500 text-white px-3 md:px-6 py-2 md:py-3 text-md md:text-lg inline-block"
          >Contact Us</a
        >
      </div>
    </div>
  </div>
</div>

<style>
  .carousel_item {
    position: relative;
    min-width: 100%;
    height: 60vh;
    transition: all 0.5s linear;
    background-repeat: no-repeat;
    background-size: cover;
    background-attachment: fixed;
  }
</style>

<script is:inline>
  const carouselItems = document.querySelectorAll(".carousel_item");

  // It will be used to shift the items  by incrementing its
  // value every 2 seconds and multiplying it by 100
  let i = 1;

  // Running the condition every 2 seconds using setInterval
  setInterval(() => {
    // Accessing All the carousel Items
    Array.from(carouselItems).forEach((item, index) => {
      // Checking if the value of i is less than the
      // number of carousel items, if it is true,
      // move all the carousel items 100% towards left
      if (i < carouselItems.length) {
        item.style.transform = `translateX(-${i * 100}%)`;
      }
    });

    // If the the value of i is less than the number of
    // Carousel items, then increment the value of i by 1
    // Otherwise set the value of i to 0 if it becomes greater
    // than the number of carousel items
    if (i < carouselItems.length) {
      i++;
    } else {
      i = 0;
    }
  }, 4000);
</script>
