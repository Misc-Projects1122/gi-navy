---

---

<div class="md:relative md:border-y-4 md:border-primary">
  <div class="carousel_container">
    <div
      class="flex flex-nowrap overflow-hidden border-b-2 border-primary md:border-b-0">
      <div class="carousel_item">
        <img src="/hero/heroBanner1.webp" class="h-full w-full" />
      </div>
      <div class="carousel_item">
        <img src="/hero/heroBanner2.webp" class="h-full w-full" />
      </div>
      <div class="carousel_item">
        <img src="/hero/heroBanner3.webp" class="h-full w-full" />
      </div>
      <div class="carousel_item">
        <img src="/hero/heroBanner4.webp" class="h-full w-full" />
      </div>
    </div>
  </div>
  <div class="md:absolute inset-0 bg-black opacity-50"></div>
  <div class="md:absolute inset-0 flex items-center justify-start md:px-16">
    <div
      class="flex flex-col items-center md:items-start text-white text-center md:text-left py-4 md:py-0 px-7 md:px-0 bg-dark md:bg-transparent max-w-xl">
      <h1
        class="text-base xxs:text-lg font-semibold md:text-5xl font-condensed">
        Pioneering Engineering, Delivering Excellence
      </h1>
      <p
        class="mt-4 text-xs md:text-lg lg:text-sm font-medium w-3/4 carousel_text">
        Mission-driven, customer-centric, multi-domain maritime solutions using
        cutting-edge technologies to envision and safeguard the fleet of the
        future.
      </p>
      <div class="mt-8 space-x-4">
        <a
          class="text-white border-2 border-white px-3 md:px-8 py-1 md:py-[8px] text-md md:text-base inline-block font-condensed font-semibold transition duration-300 ease-in-out hover:bg-primary cursor-pointer">
          LEARN MORE
        </a>

        <a
          class="text-white text-md md:text-base font-semibold px-3 md:px-8 py-1 md:py-[8px] border-2 border-secondary bg-secondary inline-block transition duration-300 ease-in-out hover:bg-primary hover:border-primary cursor-pointer font-condensed">
          CONTACT US
        </a>
      </div>
    </div>
  </div>
</div>

<style>
  .carousel_item {
    position: relative;
    min-width: 100%;
    height: 90vh;
    transition: all 0.5s linear;
    background-repeat: no-repeat;
    background-size: cover;
    background-attachment: fixed;
  }
  .carousel_text {
    line-height: 30px;
  }

  /* Media query for mobile view */
  @media (max-width: 425px) {
    .carousel_item {
      height: 30vh;
    }
    .carousel_text {
      line-height: 20px;
    }
  }
</style>

<script is:inline>
  const carouselItems = document.querySelectorAll(".carousel_item");

  // It will be used to shift the items  by incrementing its
  // value every 2 seconds and multiplying it by 100
  let i = 1;

  // Running the condition every 2 seconds using setInterval
  setInterval(() => {
    // Accessing All the carousel Items
    Array.from(carouselItems).forEach((item, index) => {
      // Checking if the value of i is less than the
      // number of carousel items, if it is true,
      // move all the carousel items 100% towards left
      if (i < carouselItems.length) {
        item.style.transform = `translateX(-${i * 100}%)`;
      }
    });

    // If the the value of i is less than the number of
    // Carousel items, then increment the value of i by 1
    // Otherwise set the value of i to 0 if it becomes greater
    // than the number of carousel items
    if (i < carouselItems.length) {
      i++;
    } else {
      i = 0;
    }
  }, 4000);
</script>
