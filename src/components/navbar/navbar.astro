---
import Container from "@components/container.astro";
import Dropdown from "./dropdown.astro";
import { Astronav, MenuItems, MenuIcon } from "astro-navbar";

const menuitems = [
  {
    title: "Home",
    path: "/",
  },
  {
    title: "What we are",
    path: "/whatWeAre",
  },
  {
    title: "What we do",
    path: "#",
    children: [
      { title: "Habitat Solutions", path: "/what-we-do/habitat-solutions" },
      {
        title: "Critical Structures",
        path: "/what-we-do/critical-structures",
      },
      {
        title: "Engineering Solutions",
        path: "/what-we-do/engineering-solutions",
      },
    ],
  },
];
---

<div class="mt-[14%] md:mt-[6%] 2xl:mt-[2% h-full">
  <header
    class="fixed top-0 left-0 w-full flex flex-col lg:flex-row justify-between md:items-center mx-auto px-4 md:px-20 py-4 md:py-6 bg-white z-50">
    <Astronav>
      <div class="flex w-full lg:w-auto items-center justify-between">
        <a href="/" class="text-lg">
          <img src="/assets/logo-black.png" class="w-16 lg:w-20" />
        </a>
        <div class="block lg:hidden">
          <MenuIcon class="w-6 h-6 text-gray-800" />
        </div>
      </div>
      <div class="flex items-center">
        <MenuItems
          class="hidden w-full h-[100vh] lg:w-auto mt-2 lg:flex lg:mt-0 text-sm">
          <ul class="flex flex-col lg:flex-row lg:gap-3">
            {
              menuitems.map((item, index) => (
                <>
                  {item.children && (
                    <Dropdown
                      title={item.title}
                      children={item.children}
                      lastItem={index === menuitems.length - 1}
                    />
                  )}

                  {/* For new update NEW mark */}
                  {!item.children && (
                    <li>
                      <a
                        href={item.path}
                        class="flex lg:px-3 items-center text-black hover:text-gray-900 md:border-r-2 lg:border-r-0 border-[#007BC7]">
                        <span class="text-lg mt-2"> {item.title}</span>
                        {item.badge && (
                          <span class="ml-1 px-2 py-0.5 text-[10px] animate-pulse font-semibold uppercase text-white bg-indigo-600 rounded-full">
                            New
                          </span>
                        )}
                      </a>
                    </li>
                  )}
                </>
              ))
            }
          </ul>

          <div class="lg:hidden flex items-center mt-3 gap-4">
            <!-- <a
              href="/contact"
              class="rounded-[4px] border-2 px-4 border-[#007BC7] text-[#007BC7]">
              Contact Us
            </a> -->
            <a
              href="/contact"
              class="flex lg:px-3 items-center text-black hover:text-gray-900 md:border-r-2 lg:border-r-0 border-[#007BC7]">
              <span class="text-lg mt-2"> Contact Us</span>
            </a>
          </div>
        </MenuItems>
        <div class="hidden lg:flex items-center gap-4">
          <a href="/contact" class="bg-[#007BC7] text-white px-6 py-3">
            Contact Us
          </a>
        </div>
      </div>
    </Astronav>
  </header>
</div>
